.pagination
    text-align: center;
    margin-top: 30px;
    .previous
        svg
            height: 1.5em;
            transform: rotate(90deg);
    .next
        svg
            height: 1.5em;
            transform: rotate(-90deg);
